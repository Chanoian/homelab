apiVersion: operators.coreos.com/v1alpha2
kind: OperatorGroup
metadata:
  name: lvms-operator
  namespace: openshift-storage
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  targetNamespaces:
    - openshift-storage
